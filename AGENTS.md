# DFScrimPlanner

Codex entry point for this project.

Read `.ai/README.md` first, then `CLAUDE.md` for the current project overview, commands, stack details, and architecture. Then read the referenced `.claude/*.md` files relevant to the task, especially `workflow.md`, `common-tasks.md`, `dev-setup.md`, `troubleshooting.md`, and any API/schema/feature docs.

Treat `.claude/settings*.json` as legacy Claude configuration only.
## Codex Standards

This project follows: `codex-sveltekit`. Read the matching Codex plugin skill/reference before changing project-type conventions, scaffolding, Docker, CI, secrets, deployment, or architecture covered by that standard.

## Shared AI Docs

Read `.ai/README.md` first for harness-neutral project knowledge. Prefer `.ai/*.md` over duplicated legacy `.claude/*.md` docs when both exist. Keep `.claude/**` for Claude-native commands, settings, hooks, skills, and legacy material.
